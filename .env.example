# Public URL the app is reachable at (no trailing slash).
# Dev: http://localhost:3000
# Prod: https://codex.pho.town
PUBLIC_URL=http://localhost:3000

# Spectrum dashboard host — device-auth, projects, lines, platforms.
# Prod:    https://app.photon.codes
# Staging: https://staging-app.photon.codes
SPECTRUM_API_HOST=https://app.photon.codes

# Spectrum cloud host — Basic-auth REST + the host spectrum-ts subscribes to.
# Auto-pairs with SPECTRUM_API_HOST when unset; override to pin explicitly.
# Prod:    https://spectrum.photon.codes
# Staging: https://staging-spectrum-cloud.photon.codes
SPECTRUM_RUNTIME_HOST=

# spectrum-ts SDK reads this to know which cloud to connect to. BARE hostname
# (no scheme). Must match SPECTRUM_RUNTIME_HOST without https://.
# Prod:    spectrum.photon.codes
# Staging: staging-spectrum-cloud.photon.codes
SPECTRUM_CLOUD_URL=

# Device-auth client_id. Must be on the dashboard's allowlist.
SPECTRUM_CLIENT_ID=photon-cli

# Postgres connection string.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/codex

# 32-byte hex master key for encrypting tenant OpenAI keys at rest.
# Generate once: openssl rand -hex 32
MASTER_KEY=

# Default model used to answer iMessage threads.
CODEX_MODEL=gpt-5-codex

# Container entrypoint mode: "webapp" or "bridge".
PROCESS=webapp
